import { PlanApplyStatusFactory } from './plan-apply-status-factory';

describe('PlanApplyStatusFactory', () => {
  it('should create an instance', () => {
    expect(new PlanApplyStatusFactory()).toBeTruthy();
  });
});
